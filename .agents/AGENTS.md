# Project-Scoped Rules

- ALWAYS automatically `git commit` and `git push` your changes to the remote repository when you have successfully completed a requested task or logical milestone. 
- DO NOT ask the user for permission to push. 
- DO NOT wait for the user to remind you to push.
